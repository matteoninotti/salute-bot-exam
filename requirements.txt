# salute-bot runtime + dev dependencies. Pinned loosely (compatible-release) to
# survive Homebrew Python patch bumps without silently rotting the venv.
# Rebuild:  python3.14 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- scraping ---
playwright~=1.48        # headless Chromium drive of the JSF flow (drive half)
beautifulsoup4~=4.12    # parse the JSF partial-response CDATA (parse half)

# --- secrets at rest (D3/D29) ---
cryptography~=44.0      # AEAD (cf_enc/nre) + HMAC blind index (cf_hash)

# --- alerts (D10/D15) ---
boto3~=1.35             # AWS SES email

# --- dev ---
pytest~=8.3
ruff~=0.15
